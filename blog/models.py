from django.db import models
from django.utils import timezone
from django.contrib.auth.models import User

# each class represents a table in a database and each attribute of class is column of table and each instance of class is row


